/* ═══════════════════════════════════
   VIDEO — sem vídeo no Lúmen
   Funções mantidas como no-op para
   evitar erros caso referenciadas.
═══════════════════════════════════ */
function toggleMute(e) { if (e) e.stopPropagation(); }
function togglePlayPause() {}
